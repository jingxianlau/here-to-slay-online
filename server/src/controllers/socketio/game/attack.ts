export const attackMonster = (
  roomId: string,
  userId: string,
  monsterId: string
) => {};

export const attackRoll = (
  roomId: string,
  userId: string,
  monsterId: string
) => {};

export const confirmAttack = (
  roomId: string,
  userId: string,
  monsterId: string
) => {};
